/** DI-токен основного клиента Redis. */
export const REDIS = Symbol('REDIS');

/**
 * DI-токен клиента, подписанного на pub/sub.
 *
 * Второй клиент здесь не прихоть: соединение, выполнившее `SUBSCRIBE`, переходит
 * в режим подписчика и обычных команд больше не принимает. Обойтись одним нельзя —
 * это правило самого протокола Redis, а не библиотеки. Соединение создаётся
 * дублированием основного (`duplicate()`), то есть с той же конфигурацией и той же
 * логической базой: отдельного пути настройки не появляется.
 */
export const REDIS_SUBSCRIBER = Symbol('REDIS_SUBSCRIBER');
