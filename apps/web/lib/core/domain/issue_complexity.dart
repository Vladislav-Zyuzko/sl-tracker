/// Сложность задачи в story points (`docs/design/system.md`, 6).
///
/// Это шкала оценки, а не срочности, поэтому цветом не кодируется:
/// иначе она конкурирует с приоритетом за внимание.
sealed class IssueComplexity {
  /// Допустимые значения. Значение может отсутствовать — тогда рисуется
  /// прочерк в тех же габаритах, чтобы колонка не прыгала.
  static const values = <int>[1, 2, 3, 5, 8, 13];
}
